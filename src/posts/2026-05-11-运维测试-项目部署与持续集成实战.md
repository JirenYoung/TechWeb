---
title: 运维测试：项目部署与持续集成实战
date: 2026-05-11
tags: [运维, CI/CD, Docker, Nginx]
categories: [运维]
cover: https://images.unsplash.com/photo-1667372393119-3d4c48d07fc9?w=800&q=80
excerpt: 本文是一篇运维测试文档，用于验证个人技术博客的文章读取、标签分类、归档展示等功能是否正常运作。
featured: true
---

## 测试目的

本文用于验证博客系统的以下功能：

1. **文章读取** — `import.meta.glob` 是否正确扫描 `src/posts/` 目录
2. **Front Matter 解析** — title / date / tags / categories / excerpt / cover / featured 全部正确解析
3. **Markdown 渲染** — marked + highlight.js 正确渲染
4. **标签筛选** — 点击「运维」「Docker」等标签应能筛选到本文
5. **分类展示** — 本文归属「运维」分类
6. **归档页面** — 本文应出现在 2026 年归档中
7. **首页展示** — featured: true，应在精选文章区出现

---

## 部署流程概览

现代前端项目的标准部署流程大致如下：

```bash
# 1. 拉取代码
git pull origin main

# 2. 安装依赖
npm ci --production

# 3. 构建项目
npm run build

# 4. 部署到 Nginx
cp -r dist/* /var/www/html/
```

---

## Docker 容器化部署

使用 Docker 可以确保环境一致性：

```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

---

## Nginx 配置示例

```nginx
server {
    listen       80;
    server_name  blog.example.com;
    root         /usr/share/nginx/html;
    index        index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
}
```

---

## CI/CD Pipeline（GitHub Actions）

```yaml
name: Deploy
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - name: Deploy to Server
        run: |
          rsync -avz dist/ user@server:/var/www/html/
```

---

## 关键检查清单

| 检查项 | 状态 |
|--------|------|
| DNS 解析正确 | ✅ |
| SSL 证书有效期 | ✅ |
| Nginx 配置语法无误 | ✅ |
| 静态资源缓存策略 | ✅ |
| Gzip 压缩已启用 | ✅ |
| 404 页面存在 | ✅ |

---

## 总结

如果这篇文章在博客中正确显示 — 标题、日期、标签（运维 / CI/CD / Docker / Nginx）、分类（运维）、封面图、代码高亮、表格渲染全部正常 — 说明博客的 Markdown 文章系统运作完全正常。之后你只需在 `src/posts/` 下新增 `.md` 文件，刷新页面即可看到新文章。
