version: "3.8"

services:
  tech-blog:
    build: .
    container_name: tech-blog
    ports:
      - "8080:80"
    restart: unless-stopped
    # 如果你需要挂载自定义 Nginx 日志目录：
    # volumes:
    #   - ./nginx-logs:/var/log/nginx
